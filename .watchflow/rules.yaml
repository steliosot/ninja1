rules:
  - id: "description-presence"
  name: "Pull Requests Must Have a Description"
  description: "Pull Requests must have a description."
  enabled: true
  severity: "medium"
  event_types: ["pull_request"]
  parameters: {}
